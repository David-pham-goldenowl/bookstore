var table = document.querySelector("table");

var row = "<%= j render partial: 'author', locals: { author: @author } %>";

table.insertAdjacentHTML("beforeend", row);

var input = document.querySelector("#author_name");
input.value = '';
